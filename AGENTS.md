# 저장소 가이드

## 프로젝트 구조와 모듈 구성

`MealLens/`에는 SwiftUI 앱, SwiftData 모델, HealthKit 연동, Core ML/Vision 추론, 이미지 리소스가 있습니다. `MealLensTests/`에는 영양 계산, 데이터 저장, 이미지 준비, 분류기 동작을 검증하는 XCTest가 있습니다. `ModelTraining/`에는 데이터셋 준비, 학습, 평가, 출처·라이선스 문서가 있습니다. 생성 데이터셋과 학습 결과는 `work/` 아래에 두고 Git에 커밋하지 않습니다. Xcode 프로젝트는 `MealLens.xcodeproj/`입니다. 모델이나 데이터 작업 전에는 `README.md`, `VALIDATION.md`, `THIRD_PARTY_NOTICES.md`를 확인하세요.

## 빌드·테스트·개발 명령

시뮬레이터 전체 테스트:

```sh
xcodebuild -project MealLens.xcodeproj -scheme MealLens \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,id=C15CB54E-B2EC-4895-834E-CD3B7D3B262C' test
```

연결된 아이폰용 빌드:

```sh
xcodebuild -project MealLens.xcodeproj -scheme MealLens -sdk iphoneos build
```

모델 학습 스크립트는 저장소의 Python 환경에서 실행합니다. 새 데이터를 추가할 때 출처와 라이선스를 기록하고, 제한된 원본 이미지나 생성된 학습 캐시는 Git에 넣지 마세요.

## 모델 개발 지침

모델 관련 코드는 `ModelTraining/`에 두고, 원본·중간 산출물·실험 결과는 `work/` 아래에 분리합니다. 데이터셋은 음식 종류와 영역 좌표를 명확히 기록하고, 같은 원본 사진의 crop이 학습·검증·시험 세트에 함께 들어가지 않도록 그룹 단위로 분리하세요. 모든 데이터셋에는 출처, 사용 허가, 수집 날짜, 클래스 목록을 남기며 Open Images처럼 이미지와 주석의 라이선스가 다르면 각각 기록합니다.

실제 사용 사례를 최대한 넓게 수집합니다. 음식 종류별로 한식·일식·중식·양식·동남아·중동·남미·유럽 음식, 디저트·음료·간식·식재료를 포함하고, 각 종류에 정면·사선·근접·먼 거리, 밝은/어두운 조명, 식당·가정·포장 용기, 부분 가림, 여러 그릇, 빈 그릇과 빈 접시를 별도 사례로 확보하세요. 빈 그릇·식기·테이블은 음식이 아닌 hard-negative로 라벨링하고, 학습 후 confusion matrix에서 발견되는 모든 혼동 조합을 별도 검증 묶음으로 유지합니다. 특정 음식 몇 가지를 고정된 목표로 삼지 말고, 새 데이터에서 반복적으로 나타나는 오인식과 낮은 recall 클래스를 우선 보강합니다. 데이터 수를 늘릴 때는 클래스별 최소 표본 수를 맞추고, 특정 출처나 촬영 스타일에 편중되지 않게 합니다.

인터넷 수집은 공식 배포처와 명시적 라이선스가 있는 데이터만 사용합니다. Open Images처럼 이미지별 라이선스 메타데이터가 제공되는 경우 원본 URL·저작자·라이선스를 함께 보존하고, 검색엔진 결과나 라이선스가 불명확한 웹 이미지를 직접 긁어오지 않습니다. 연구 전용·비상업 라이선스 데이터는 앱에 포함할 모델 학습에 사용하지 않고 별도 실험으로 격리합니다.

확장 데이터는 먼저 `download_openimages_food.py`와 기존 데이터 준비 스크립트로 수집한 뒤, 해시·원본 사진 ID 기준으로 중복을 제거합니다. 새 모델은 `work/runs/<이름>-v<버전>`에 학습하고, `prepare_openimages_classifier.py`로 누출 방지 분할을 만듭니다. 증강을 사용하면 밝기·색온도·회전·좌우 반전·부분 가림을 적용하되 음식 이름이나 영역 좌표가 바뀌지 않는 범위로 제한합니다. 학습 전후에 실제 앱과 동일한 `PhotoPreparation` 및 Vision/Core ML 경로로 평가하고, 빈 그릇 오탐률과 여러 음식 사진의 음식별 recall을 별도로 기록합니다.

모델 개선의 목표는 음식 분류와 중량·칼로리 추정을 각각 정확하게 높이는 것입니다. 분류 모델은 음식별 top-1/top-3 정확도, precision, recall, confusion matrix를 기록하고, 평가 결과에서 확인되는 모든 혼동이 잦은 쌍과 비음식 hard-negative를 별도 시험 세트로 관리합니다. 낮은 확신도 결과는 억지로 특정 음식으로 확정하지 않고 후보 또는 확인 필요 상태로 표시합니다.

중량 모델은 사진의 실제 제공량 라벨을 사용해 grams MAE와 음식군별 오차를 측정하고, 칼로리 모델은 실제 kcal 라벨로 kcal MAE, RMSE, 평균 절대 백분율 오차를 측정합니다. 중량과 칼로리를 한 모델의 단일 대리값으로 취급하지 말고 별도 출력으로 학습·검증하세요. 여러 음식 사진은 영역별 중량 합계와 전체 중량이 일치하는지, 영역별 칼로리 합계와 전체 칼로리가 일치하는지 검증합니다. 기존 모델보다 시험 세트 오차가 줄고 실제 앱 전처리 경로에서도 재현될 때만 새 Core ML 모델을 배포합니다.

분류 모델은 학습 정확도만으로 채택하지 않습니다. 고정된 검증·시험 세트에서 클래스별 precision/recall, confusion matrix, 다중 음식 사진 성능을 확인하고, 기존 모델과 실제 앱 전처리 경로를 비교하세요. 중량·칼로리 모델은 MAE와 대표 음식별 오차를 기록하며, 시험 세트 결과가 악화되면 새 모델을 배포하지 않습니다. 사진 기반 값은 항상 추정치로 표시합니다.

Core ML 모델은 메타데이터에 라벨 매핑, 데이터 출처, 학습 버전, 평가 지표를 포함해야 합니다. 앱에서는 새 모델을 먼저 선택적 보정 모델로 연결하고, 낮은 확신도에서는 기존 모델 결과를 유지하세요. 모델을 실제 사용 가능하도록 개선하는 것이 최우선 목표입니다. 학습 결과는 평가를 통과하고 앱 동작에 실제로 반영한 모델만 Xcode 프로젝트 리소스로 포함해 커밋합니다. 모델 파일, 프로젝트 리소스 등록·빌드 단계, 모델 카드, 데이터셋 라이선스·출처, 학습 명령, 검증 결과, 모델 용량을 함께 확인하고 커밋하세요. 단순 실험 모델과 미사용 학습 산출물은 `work/`에 보관하며 커밋하지 않습니다. GitHub 파일 크기 제한을 넘는 앱 사용 모델은 Git LFS 등 저장소 규칙에 맞는 방식을 사용합니다.

## 코딩 스타일과 이름 규칙

Swift 기본 형식을 따르고 들여쓰기는 공백 4칸을 사용합니다. 타입은 `UpperCamelCase`, 메서드·프로퍼티·지역 변수는 `lowerCamelCase`로 작성합니다. SwiftUI 조합, 값 타입, `async/await`, 추론 작업의 actor 격리를 우선하세요. 기존 화면처럼 사용자에게 보이는 문구는 한국어로 작성합니다. 테스트 이름은 `test<동작>()` 형식을 사용하고 운영 코드의 강제 언래핑은 피하세요.

## 테스트 지침

테스트 프레임워크는 XCTest이며 `xcodebuild test`로 실행합니다. 분류기 임계값, 여러 음식 영역의 중량 배분, 수정 가능한 영양값, SwiftData 저장 동작에 회귀 테스트를 추가하세요. 전체 시뮬레이터 테스트와 아이폰 빌드가 성공해야 하며, 모델을 바꾸면 별도 검증 세트의 평가 결과도 기록해야 합니다.

## 커밋과 풀 리퀘스트 지침

사용자에게 보이는 변경을 짧은 명령형 제목으로 작성하세요. 예: `사진 추정 개선 및 음식 선택 단순화`. 풀 리퀘스트에는 변경 동작, 실행한 검증 명령, 모델 지표, 관련 이슈, UI 변경 스크린샷을 포함하세요. 데이터셋 라이선스나 출처에 영향이 있으면 설명하고 `.gitignore`로 제외한 파일도 명시하세요.

## 아키텍처와 데이터 보호

추론은 오프라인 우선입니다. Vision과 Core ML은 기기에서 실행하고, SwiftData는 식사 기록과 사진을 로컬에 저장합니다. 영양값과 사진 기반 중량은 추정치이며 의료 조언이 아닙니다. API 키, 개인 HealthKit 데이터를 저장소에 추가하지 마세요.
